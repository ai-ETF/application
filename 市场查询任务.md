## 任务（必读）

已验证：当前后端支持添加ETF指令、查询ETF费率等指令。[后端全部支持指令详见](/请求体格式.md)

现在我要添加一个ETF到自选中， 我需要使用 `curl` 指令实现，那你的任务就是把这个变成可以直接在前端通过页面交互实现这个操作。

当然不限于添加到自选这个操作，你需要完成所有后端支持的指令的操作，不懂就问，有任何想法就直接提出。

这边建议你下载一个“雪球”或者“同花顺”，看看这些软件是如何实现类似的功能的。
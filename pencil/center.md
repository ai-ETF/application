下面是 **Claude / MCP / Pencil Agent 可直接执行的完整高质量 Prompt（V3 · Personal Center Warm）**。
已经完全符合你现在的 **AI ETF 小程序 technical warm 体系 + #F9F6F0 统一背景**。

---

# 📌 Claude MCP / Pencil Agent 执行指令

## 页面：Personal Center（个人中心 · 我的）

## App：智析ETF

## Design System：technical warm

## Version：V3

---

# 🔒 全局执行规则（必须遵守）

1. 在当前文件中创建一个新的移动端画布。
2. 不创建额外页面。
3. 不调用截图工具。
4. 不进行视觉分析。
5. 所有布局必须使用 Auto Layout。
6. 严禁使用绝对定位。
7. 不允许元素重叠。
8. 所有组件必须来自组件库：

```
mobile-03-technicalwarm_light
```

9. 若组件不存在，使用最接近组件组合。
10. 所有间距遵循 8pt 系统（8 / 16 / 24 / 32）。
11. 所有容器必须设置 Fill 或 Hug。
12. 明确父子关系。
13. 适配移动端竖屏。
14. 视觉风格：Minimalist + Technical Warm。
15. 圆角统一。
16. 完成后直接返回成功。

---

# 🪜 第一步：创建画布（核心）

创建移动端竖屏画布：

* 宽度：390
* 高度：844
* 方向：Vertical

设置页面背景：

```
#F9F6F0
```

字体：

```
sans-serif
```

---

# 🪜 第二步：创建页面主容器

创建页面 Root 容器：

* Vertical Auto Layout
* Fill 宽度与高度
* 子元素间距：0
* 不设置背景
* 父级为画布

该容器为整个页面结构。

---

# 🪜 第三步：页面整体结构

Root 下包含：

1. Status Bar
2. Header（头像与用户名）
3. 内容主区域

---

# 🪜 第四步：Status Bar

创建横向 Auto Layout：

* 高度：44
* Fill 宽度
* 左右两端对齐
* 垂直居中
* 左右内边距：16

---

## 左侧时间

文本：

* 内容：16:26
* 字号：16
* 颜色：

```
#1A1A1A
```

---

## 右侧状态图标组

创建横向容器：

* Hug 宽度
* 子元素间距：8

包含：

* WiFi
* Cellular
* Battery
* Charging

颜色统一：

```
#1A1A1A
```

---

# 🪜 第五步：Header（用户信息）

创建 Vertical Auto Layout：

* Fill 宽度
* 左右内边距：16
* 上下内边距：24
* 子元素间距：0

内部创建横向 Auto Layout：

* 左右两端对齐
* 垂直居中

---

## 5.1 左侧用户区域

创建横向容器：

* 子元素间距：16
* 垂直居中

---

### 头像

组件：

* 圆形
* 尺寸：60
* 背景：

```
#EFEAE2
```

图标颜色：

```
#C07A4A
```

---

### 用户名

文本：

* 内容：Мурманск
* 字号：24
* 字重：Semibold
* 颜色：

```
#1A1A1A
```

---

## 5.2 右侧图标区

创建横向 Auto Layout：

* 子元素间距：16

包含：

1. 通知图标
2. 更多菜单

图标颜色：

```
#1A1A1A
```

通知带红点 Badge。

---

# 🪜 第六步：内容主区域

创建 Vertical Auto Layout：

* Fill 宽度
* 左右内边距：16
* 子模块间距：16

模块顺序：

1. 我的持仓卡片
2. 风险画像
3. 订阅任务
4. 意见反馈

---

# 🪜 第七步：我的持仓卡片

创建卡片容器：

* Vertical Auto Layout
* Fill 宽度
* 内边距：20
* 子元素间距：16

视觉：

* 背景：#FFFFFF
* 圆角：16
* 边框：

```
1px #E8E3DA
```

* 轻阴影。

---

## 7.1 标题

文本：

* 我的持仓
* 字号：20
* 字重：Medium
* 颜色：

```
#1A1A1A
```

---

## 7.2 金额区

创建横向 Auto Layout：

* 子元素间距：8
* 垂直底部对齐

金额：

* 0.00
* 字号：36
* 字重：Bold
* 颜色：

```
#1A1A1A
```

单位：

* 元
* 字号：18
* 颜色：

```
#8C857A
```

---

# 🪜 第八步：功能入口卡片（统一组件）

创建统一 Entry Card 组件。

所有入口必须使用同一结构。

---

## Entry Card 结构

横向 Auto Layout：

* Fill 宽度
* 左右两端对齐
* 垂直居中
* 内边距：20

视觉：

* 背景：#FFFFFF
* 圆角：16
* 边框：1px #E8E3DA

点击状态：

```
#F4F1EA
```

---

## 左侧区域

横向 Auto Layout：

* 子元素间距：16
* 垂直居中

---

### 图标

尺寸：24
颜色：

```
#C07A4A
```

---

### 文本

字号：18
字重：Medium
颜色：

```
#1A1A1A
```

---

## 右侧区域

根据不同入口变化：

---

# 🪜 第九步：风险画像

文本：

* 风险画像

右侧：

* Arrow
* 颜色：

```
#BFB7AC
```

---

# 🪜 第十步：订阅任务

文本：

* 风险画像测评

右侧：

* 去测评
* 颜色：

```
#C07A4A
```

* Arrow。

---

# 🪜 第十一步：意见反馈

文本：

* 意见反馈

右侧：

* Arrow
* 颜色：

```
#BFB7AC
```

---

# 🪜 第十二步：空间节奏优化

确认：

* Header → 持仓卡片：24
* 卡片之间：16
* 内部：16

保证呼吸感。

---

# 🪜 第十三步：设计一致性检查

必须确认：

1. 全部 Auto Layout。
2. Fill 与 Hug 正确。
3. 无绝对定位。
4. Warm 视觉统一。
5. 卡片层级清晰。
6. 字体层级稳定。
7. 阴影一致。
8. 图标统一。
9. 品牌 Accent 色一致。

---

# ✅ 完成规则

完成后：

* 不截图
* 不解释
* 不分析
* 直接返回成功


```
my-page
```

